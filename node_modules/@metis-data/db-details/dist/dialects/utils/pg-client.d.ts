import { Client } from 'pg';
import { Connection } from '@types';
interface ClientConfig {
    host: string;
    database: string;
    user: string;
    password: string;
}
export declare class MPgClient {
    private client;
    constructor(clientConfig: ClientConfig | Connection | Client);
    queryDatabase: (query: string) => Promise<any>;
    endClient: () => Promise<void>;
}
export {};
